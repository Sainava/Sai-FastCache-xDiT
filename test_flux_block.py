from diffusers.models.transformers.transformer_flux import FluxTransformerBlock
# We just need to inspect the class properties
print("hidden_size in dir?", "hidden_size" in dir(FluxTransformerBlock))
print("dim in dir?", "dim" in dir(FluxTransformerBlock))
